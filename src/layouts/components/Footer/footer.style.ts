import type { CSSProperties } from 'react'

const footer: CSSProperties = {
  textAlign: 'center',
  paddingInline: '16px',
  paddingBlock: '10px',
  background: 'transparent'
}

export const style = {
  footer
}
